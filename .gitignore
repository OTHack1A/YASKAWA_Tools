# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/*.log
*.log

# PyInstaller build artifacts
build/
*.spec.bak

# Compiled binaries & build output — distributed via GitHub Releases, never in the repo.
# Ignore the whole dist/ tree (covers dist/Melo/ and any other --distpath target).
/dist/
dist/YaskawaTools
dist/YaskawaTools.exe
dist/YaskawaTools.dmg

# Local AI scratch notes / nested helper repo — private, never in the repo
AI-noters/

# Code-signing material must never be committed
*.pfx
*.p12

# Environment
.env
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Auth state (generated at runtime)
auth_state.json
auth_state.json.tmp

# Test output
test_params.pdf
*.pdf

# Robot controller dump — dati privati, non devono MAI finire nella repo
R1/
r1/

# Reference screenshots from the real teach pendant — privati, mai nella repo
References/
references/

# Private test material: real controller exports + pendant screenshots — mai nella repo
tests/*.png
tests/*.CND
tests/*.cnd
